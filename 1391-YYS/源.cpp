#include<stdio.h>
int main()
{
    int jw, w, t, n, i, j, z, T;
    //	Jw?????? W?????? a?????????? T???????????? 
    scanf("%d", &T);
    while (T--)
    {
        int a[1000] = { 1 };
        scanf("%d", &n);
        w = 1, j = 1;
        for (i = 1; i <= n; i++)
        {
            for (j = 1, jw = 0; j <= w; j++) {
                t = a[j - 1] * i + jw;	// ?????????????????????????????? i ????????????????  
                a[j - 1] = t % 10;		// ?????????????????? 
                jw = t / 10;   			//?????? 
            }

            while (jw) {
                a[++w - 1] = jw % 10;
                jw /= 10;
            }
            //??????????????????????????????		
        }
        for (z = w; z >= 1; z--) {
            printf("%d", a[z - 1]);
        }
        printf("\n");
    }
    return 0;
}