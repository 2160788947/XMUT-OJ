#include<stdio.h>
int main()
{
	int N, M, ci[1005] = { 0 }, di[1005] = {0}, L,i;
	scanf("%d %d %d", &N, &M, &L);
	while (M--)
	{
		scanf("%d %d", &ci[i], &di[i]);
		i++;
	}
	while (M > 0)
	{
		if()
	}

	return 0;
}